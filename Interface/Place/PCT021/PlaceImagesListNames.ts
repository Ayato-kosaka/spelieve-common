import { PCT021PlaceImagesListInterface } from './index';

/**
 * Export Object of interface names of PCT021PlaceImagesList.
 */
export const PCT021PlaceImagesListNames: { [P in keyof PCT021PlaceImagesListInterface]: string } = {
    imageUrl: 'imageUrl',
    originalUrl: 'originalUrl',
}
