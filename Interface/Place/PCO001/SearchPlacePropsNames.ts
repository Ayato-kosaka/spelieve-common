import { PCO001SearchPlacePropsInterface } from './index';

/**
 * Export Object of interface names of PCO001SearchPlaceProps.
 */
export const PCO001SearchPlacePropsNames: { [P in keyof PCO001SearchPlacePropsInterface]: string } = {
    onAutoCompleteClicked: 'onAutoCompleteClicked',
    hideDistinct: 'hideDistinct',
}
