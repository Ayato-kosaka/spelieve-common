import { PDB02PlaceImagesInterface } from './index';

/**
 * Export Object of interface names of PDB02PlaceImages.
 */
export const PDB02PlaceImagesNames: { [P in keyof PDB02PlaceImagesInterface]: string } = {
    imageUrl: 'imageUrl',
    originalUrl: 'originalUrl',
}
