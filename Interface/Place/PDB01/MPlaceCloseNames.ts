import { PDB01MPlaceCloseInterface } from './index';

/**
 * Export Object of interface names of PDB01MPlaceClose.
 */
export const PDB01MPlaceCloseNames: { [P in keyof PDB01MPlaceCloseInterface]: string } = {
    day: 'day',
    time: 'time',
}
