import { PDB01MPlaceOpenInterface } from './index';

/**
 * Export Object of interface names of PDB01MPlaceOpen.
 */
export const PDB01MPlaceOpenNames: { [P in keyof PDB01MPlaceOpenInterface]: string } = {
    day: 'day',
    time: 'time',
}
