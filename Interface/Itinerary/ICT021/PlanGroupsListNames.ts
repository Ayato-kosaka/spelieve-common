import { ICT021PlanGroupsListInterface } from './index';

/**
 * Export Object of interface names of ICT021PlanGroupsList.
 */
export const ICT021PlanGroupsListCols: { [P in keyof ICT021PlanGroupsListInterface]: string } = {
    plans: 'plans',
    representativePlanID: 'representativePlanID',
    representativeStartTime: 'representativeStartTime',
}