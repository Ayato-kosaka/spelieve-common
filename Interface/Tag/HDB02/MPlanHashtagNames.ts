import { HDB02MPlanHashtagInterface } from './index';

/**
 * Export Object of interface names of HDB02MPlanHashtag.
 */
export const HDB02MPlanHashtagCols: { [P in keyof HDB02MPlanHashtagInterface]: string } = {
    attachedCount: 'attachedCount',
    stayTime: 'stayTime',
}