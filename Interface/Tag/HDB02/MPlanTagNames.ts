import { HDB02MPlanTagInterface } from './index';

/**
 * Export Object of interface names of HDB02MPlanTag.
 */
export const HDB02MPlanTagCols: { [P in keyof HDB02MPlanTagInterface]: string } = {
    attachedCount: 'attachedCount',
    stayTime: 'stayTime',
}